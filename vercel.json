{
  "version": 2,
  "buildCommand": "pnpm install && pnpm build",
  "installCommand": "pnpm install",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/v0.6/:path*", "destination": "/v0.6/:path*" },
    { "source": "/:path*", "destination": "/:path*" }
  ]
}