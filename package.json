{
  "name": "crawlab-docs",
  "version": "0.7.0",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}